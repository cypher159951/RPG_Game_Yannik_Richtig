import kotlin.random.Random
/*
class KleinerHelferJohn(name: String, leben: Int) : InGame(name, leben) {
    fun attackeSchwaechung(characters: InGame) {
        val schaden = Random.nextInt(100, 150)
        characters.schaden(schaden)
        println("${name} führt die Attacke Schwächung aus und verursacht $schaden Schaden bei ${characters.name}")
    }
}

 */